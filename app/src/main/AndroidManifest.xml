<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gleibson.calcfacil">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/seta_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegraDeTres"
            android:label="@string/regra_tres" />
        <activity
            android:name=".RetiraPorcentagem"
            android:label="@string/retirar_porcentagem" />
        <activity
            android:name=".SomaPorcentagem"
            android:label="@string/somar_porcentagem" />
        <activity
            android:name=".OperDecimais"
            android:label="@string/operar_decimais" />
        <activity
            android:name=".DivisaoFracao"
            android:label="@string/divisao_fracao">
        </activity>
        <!-- ATTENTION: This was auto-generated to add Google Play services to your project for
             App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information. -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>

</manifest>
